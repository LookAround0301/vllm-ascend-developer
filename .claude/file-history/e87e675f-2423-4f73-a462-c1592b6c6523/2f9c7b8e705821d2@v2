# Offload 适配分离式 SFA Indexer — 改造总览

> 范围：`vllm-ascend/`（分支 `feat/sfa-offload-rebase-on-main`）。记录从合并 pr-11647 到让 AscendStore offload 路径兼容分离式 SFA indexer 的全部改动与适配工作。
> 对应 commit：`7d197056a`（merge pr-11647）→ `75e53e296`（offload 适配）。

---

## 0. 背景

### 0.1 pr-11647 做了什么
pr-11647「SFA indexer spec refactor」把 SFA 的 indexer cache 从**打包在 MLA spec 里**（`AscendMLAAttentionSpec.sparse_head_dim`）拆成**独立的 `AscendSFAIndexerCacheSpec`**：

- 之前：indexer 的 K 混在每层 MLA cache tuple 里（indexer 层 = `(mla_k, mla_v, indexer_k)`，非 indexer 层 = `(mla_k, mla_v)`），靠 `sparse_head_dim` 区分。
- 之后：indexer 成为独立的 `kv_caches` 条目（`model.layers.N.self_attn.indexer.k_cache`），spec 类型 `AscendSFAIndexerCacheSpec`，注册到 `FullAttentionManager`。

由于 MLA spec 与 indexer spec 都是 `FullAttentionSpec` 派生、相同 `block_size`，vLLM 的 `UniformTypeKVCacheSpecs.is_uniform_type` 会**把它们合并进同一个 uniform kv_cache_group**（`page_size_bytes = MLA + indexer`，共享 block id）。这是 pr 的设计意图（indexer 与 MLA 共用 block id）。

### 0.2 为什么 offload 必须跟着改
AscendStore 的 `KVPoolWorker` / `kv_transfer.py` 原本是**「N 层 × 每层 1 个 cache」的扁平模型**：

- `num_layers = N`，`caches_per_layer = 总条目 / N`，假设**均匀**。
- `LayerBatchBuilder._build_transfer_arrays(layer_id)` 用 `layer_id * caches_per_layer` 做均匀切片。

分离式 indexer 让 `kv_caches` 变成 **N 条 MLA + M 条 indexer（M ≤ N）**：
- 旧模型里 indexer 打包在 MLA 内，AscendStore 看到的还是每层一个 tuple —— 现在 indexer 散出去了，扁平假设全面失效。

### 0.3 三条 offload 路径（务必区分）
| 脚本 | 连接器 | 关键开关 | indexer 形态 |
|---|---|---|---|
| `prefill_kv_offload_lxs.sh` | `AscendStoreConnector` | `use_layerwise=true`，**无 `use_offload`** | 稀疏（仅部分层）|
| `prefill_kv_offload.sh` | `AscendStoreConnector` | 同上（decode 侧）| 稀疏 |
| `decode_kv_offload.sh` | `SFAKVOffloadConnector` | `use_offload=true` | pad 到所有层 |

**关键澄清**：prefill offload **不需要** `use_offload`；`use_offload` 是另一条 SFA 路径。本次工作主要面向前两条（AscendStoreConnector，GLM-5.2，`enable_sparse_c8=false` 先适配）。

---

## 1. pr-11647 merge 适配

`git merge pr-11647` 产生 5 处冲突 + 若干 auto-merge bug，全部解决后 22 个 UT 通过。

### 1.1 冲突与修复
| 位置 | 冲突 | 处理 |
|---|---|---|
| imports | pr 删了 `from dataclasses import field`，但 HEAD 函数体仍用 `field(default_factory=…)` | 恢复 `field` import（否则 `NameError`）|
| `AscendMLAAttentionSpec` | HEAD 有完整 `sparse_head_dim` 等字段，pr 把它瘦身 | 保留 HEAD 的完整定义，再叠加 pr 的 `AscendSFAIndexerCacheSpec` + `FullAttentionManager` 注册 |
| `sparse_head_dim` setup | 3 处分配逻辑冲突 | 逐处按 HEAD 语义恢复 |
| `kv_cache_interface` 分配 | indexer 分配分支冲突 | 按 pr 的分离式分支 |

### 1.2 关键修复：`sparse_kv_cache_has_indexer`
pr 从 `vllm_ascend/utils.py` 删掉了 `sparse_kv_cache_has_indexer` 及其 import，但 HEAD 的 layerwise 复用方法仍在调用它 → `NameError`。
- 重新加回 `utils.py`，并**增强**为同时识别两种 indexer 形态：新的 `AscendSFAIndexerCacheSpec`（isinstance）+ 旧的 `sparse_head_dim[2] > 0`（legacy）。
- 这个判断是后续 layerwise 复用「按 layout class 分组」的基石。

### 1.3 测试
- `tests/ut/core/test_kv_cache_interface.py`：增删对齐 UT。
- `tests/ut/worker/a2/test_model_runner_v1.py`：补 `use_offload` / `parallel_config` fixture（pr 的测试绕过了 `__init__`，缺少这些属性会崩）。
- 共 22 UT 通过。

---

## 2. offload indexer 迁移（分离式，一步到位）

目标（用户要求）：offload 路径也用分离式 `AscendSFAIndexerCacheSpec`，**彻底独立**（pr 结构），indexer cache 复用必须支持（否则分配浪费）。架构上选「B：分离，不打包」。

### 2.1 `core/kv_cache_interface.py`
- 给 `AscendSFAIndexerCacheSpec` 增加：
  - `page_bytes_per_token: int | None` 字段 —— 仅 C8 统一池时用来表达 `real_page_size_bytes` 公式无法描述的字节混合布局（int8 index + raw pad + scale）；非 C8 时为 `None`，回退真实 `real_page_size_bytes`。
  - `page_size_bytes` property 的 short-circuit 分支。
  - `merge()` 把 `page_bytes_per_token` 纳入一致性断言与合并。
- `make_offload_indexer_mla_spec` 返回类型改为 `AscendSFAIndexerCacheSpec`（用字符串注解 `-> "AscendSFAIndexerCacheSpec"` 规避前向引用 `NameError`）。

### 2.2 `worker/model_runner_v1.py`
- `get_kv_cache_spec`：offload indexer 分支改用 `make_offload_indexer_mla_spec`。
- `_allocate`：移除「跳过 indexer」逻辑，indexer 作为独立 spec 分配。
- `_reshape`：offload 分支只 gate MLA，indexer 走跨层 fetch；补 indexer `kv_caches` 条目 fallback。
- `initialize_attn_backend`：移除 `if 'indexer' in layer_names[0]: continue` 的整组跳过（逐层路由已把 indexer 交给 `AscendSFAIndexerBackend`，该跳过多余且依赖顺序）。

### 2.3 非 C8 page pad 的回退（重要）
过程中曾给非 C8 indexer 加过一个对齐 pad（288 B vs 真实 256 B）。**它崩了 indexer 的 reshape**：
- 根因：这条路径**不走 `unify_kv_cache_spec_page_size`**（MLA+indexer 已在同一个 UniformType group 里），pad 没有意义，反而在 `model_runner_v1.py` 的 `sum_page_size_bytes % page_size_bytes != 0` 处断言失败。
- 处理：**彻底删掉** `offload_indexer_aligned_bytes_per_token` 及其用法/import/UT，非 C8 indexer 回到 `page_bytes_per_token=None`（真实页大小）。

---

## 3. AscendStore 改造（核心）

### 3.1 根本问题
分离式 indexer 让 AscendStore 的三个假设全失效：

| 假设 | 失效表现 |
|---|---|
| `num_layers = N` | `sum(group_num_layers)` 把 indexer 当独立层 → **2N 膨胀**，破坏扁平寻址 + step-end save drain |
| 每层 `caches_per_layer` 均匀 | indexer 稀疏（GLM-5.2 只有部分层有）→ **非均匀** |
| worker 与 scheduler 的 `page_size_bytes` 一致 | worker 按单 MLA 算，scheduler 按合并 SUM 算 → GVA 偏移错位 |

### 3.2 方案演进
1. **尝试 A：均匀捆绑**（每层 MLA+indexer 一起）。实现后**启动崩溃**：
   `AscendStore transformer-layer layout requires a uniform number of cache legs per transformer layer; layer 3 has 2, expected 3.`
   —— 因为 GLM-5.2 的 indexer **按层稀疏**：`get_kv_cache_spec` 的 prefill-offload 分支（`use_offload=false`）只在真正带 `DeepseekV32IndexerCache` 模块的层上建 indexer spec（`model_runner_v1.py:5616`），不像 use_offload 路径会 pad 到所有层（`5499-5507`）。所以 indexer 层 = 3 leg，纯 MLA 层 = 2 leg，不可能均匀。
2. **尝试 B：把 indexer pad 到所有层**。被否：与 reuse 叠加有共享 buffer 语义风险，且语义上非 indexer 层根本没有 indexer。
3. **最终 C：per-layer 变长布局**（见 3.3）。改动面最小、语义最干净、正确支持稀疏 + 复用。

### 3.3 最终设计：transformer-layer 变长布局
核心认识：**GVA 侧每层页必须均匀（= MLA+indexer，调度器合并页），但 HBM 侧每层 leg 数可以不均**。

#### `pool_worker.py :: _infer_transformer_layer_group_metadata`
- 解析每条 `kv_caches` key 的 transformer 层号 + 是否 indexer。
- 按 transformer 层 L 输出变长 leg：`[MLA(L=0) legs…, idx(L=0) legs…, MLA(L=1) legs…, …]`（层内 MLA 在前、indexer 在后）。
- 构建并返回：
  - `layer_offsets[L]` —— L 在扁平数组里的起始下标；
  - `layer_legs[L]` —— L 的 leg 数（indexer 层 3、纯 MLA 层 2）；
  - `page_size_bytes = max(各层 leg 字节和) = MLA+indexer`（= scheduler 合并页）。
- `group_num_layers[0] = N`，**不再覆盖 `num_layers`**（保持 N）。
- 去掉「均匀 leg」断言；改为每层至少有 MLA leg 的保护性检查。

#### `kv_transfer.py :: LayerBatchBuilder`
- 读 `token_database.group_layer_offsets/legs`（有则变长切片）；无表时回退均匀 `// num_layers`（兼容旧路径/UT）。
- `_build_transfer_arrays(layer_id)`：`base_offset = layer_offsets[layer_id]`、`cpl = layer_legs[layer_id]`，切变长窗口。
- `rank_layer_offset = (layer_id * num_ranks_per_layer + key) * page_size_bytes` **不变**（均匀页）。
- 断言改为「所有层窗口不越界」。

#### `config_data.py :: ChunkedTokenDatabase`
- 新增 `group_layer_offsets / group_layer_legs` 字段，经 `set_group_buffers` 透传给 builder。

#### GVA 一致性
- 每个 GVA block = `num_layers × num_ranks × page_size_bytes`，`page_size_bytes = MLA+indexer` 对所有层均匀。
- indexer 层：GVA 页覆盖 MLA 区 + indexer 区，全量搬。
- 非 indexer 层：只搬 MLA 区，indexer 区空置（DRAM，可接受），永远不会被该层 attention 读到。

### 3.4 修复的 bug 清单
| Bug | 现象 | 修复 |
|---|---|---|
| `num_layers` N→2N 膨胀 | 扁平寻址错、save drain 超时 | `register_kv_caches` 不再用 `sum(group_num_layers)` 覆盖 `num_layers`，保持 N |
| worker↔scheduler `page_size_bytes` 不一致 | GVA 偏移错位 | worker 用 `max(各层 leg 字节和) = MLA+indexer`，与 connector（`kv_cache_groups[0].kv_cache_spec.page_size_bytes`）一致 |
| layerwise 合并膨胀 ~20× → OOM | 复用未生效，buffer 数算错 | 见 §4（class-aware 复用 + 硬 assert）|
| 稀疏 indexer 启动崩溃 | 均匀 leg 断言失败 | 变长布局（本节）|

---

## 4. layerwise 复用（class-aware）

`layerwise_num_shared_buffers=2`：independent 层（首/尾）+ 中间层共享 nsb buffer。分离式 indexer 后变成 2N 条目，必须按 **layout class** 分组复用，否则 indexer 不参与复用 = 分配浪费。

### 4.1 `get_layerwise_num_tensors`（内存膨胀因子）
改为 class-aware：
```
num_tensors = num_layout_classes × (len(independent_layers) + num_shared_buffers)
```
layout class = `(spec 类型, 是否 sparse_c8, 是否带 indexer)`。worker 用它精确反推 `num_blocks` 膨胀因子，避免 OOM 或欠分配。并加**硬 assert**：合并后 buffer 数必须等于它，否则报错。

### 4.2 `_merge_kv_cache_tensors_for_layer_reuse`
- 移除「`len(layer_names) != total_layers` 就 return」的逃逸（2N 时会误退）。
- 按 `_layout_class` 把条目分区（MLA 类 N 成员、indexer 类 M 成员），**每类各自**做 independent（首/尾）+ nsb 轮转共享。
- reuse-mate map 改为按 **transformer 层 0..N-1** 建 key：`{L: L - nsb}`，MLA(L) 与 indexer(L) 作为同一个 AscendStore 层一起加载、各自复用本类 buffer。

效果：MLA 物理缓冲 = `2+nsb`，indexer 物理缓冲 = `2+nsb`，**无浪费**，且 indexer 复用被真正支持（满足用户硬要求）。

---

## 5. 数据流（改造后）

**Save（prefill 期间 HBM→GVA 池）**
1. 第 L 个 transformer 层 MLA forward 结束 → `maybe_save_kv_layer_to_connector`（`sfa_v1.py`）。
2. `save_kv_layer(current_layer=L)` → sending thread `batch_copy` 该层全部 leg（MLA k/v + indexer k，若 L 是 indexer 层）。
3. indexer **无自己的 attention forward / save hook** —— 它作为所在 transformer 层的副产物被一并保存。
4. `L = N-1` 时触发 step-end save drain。

**Load（prefix 命中时 GVA→HBM）**
1. `wait_for_layer_load(L)`（按 transformer 层 reuse-mate gate）。
2. recv thread `batch_copy` 该层全部 leg 回 HBM。
3. indexer K 在 MLA forward 读它之前已回灌（非 indexer 层无此步）。

---

## 6. 文件改动清单（commit `75e53e296`）

| 文件 | 改动 |
|---|---|
| `core/kv_cache_interface.py` | `AscendSFAIndexerCacheSpec` 加 `page_bytes_per_token`/property/merge；`make_offload_indexer_mla_spec` 返回该类型；删非 C8 pad helper |
| `worker/model_runner_v1.py` | offload indexer 迁移到分离 spec；`_allocate`/`_reshape`/`get_kv_cache_spec` 适配；删 `initialize_attn_backend` 整组跳过；layerwise 复用 class-aware + `get_layerwise_num_tensors` 公式 |
| `distributed/.../ascend_store/pool_worker.py` | `_infer_transformer_layer_group_metadata` 改变长布局；`register_kv_caches` 保持 `num_layers=N`、用返回的 `page_size_bytes`、透传 layer 表 |
| `distributed/.../ascend_store/kv_transfer.py` | `LayerBatchBuilder` 变长切片（`layer_offsets/legs`），均匀回退 |
| `distributed/.../ascend_store/config_data.py` | 新增 `group_layer_offsets/legs` 字段 + `set_group_buffers` 透传 |
| `utils.py` | 重新加回并增强 `sparse_kv_cache_has_indexer`（两种 indexer 形态）|
| `tests/ut/core/test_kv_cache_interface.py` | 对齐 UT（删非 C8 pad 相关）|
| `tests/ut/worker/a2/test_model_runner_v1.py` | offload indexer UT + fixture |
| `tests/ut/distributed/ascend_store/test_pool_worker.py` | bundle 测试改新契约 + 新增**稀疏 indexer**布局测试（崩溃现场）|
| `tests/ut/distributed/ascend_store/_mock_deps.py` | 注入 `backend_map`，解锁 `pool_worker` 在 mock 下导入 |

共 10 文件，+484 / −103。

---

## 7. 验证状态

| 项 | 结果 |
|---|---|
| 改动文件编译 | 全部 OK |
| `test_kv_cache_interface` + `test_model_runner_v1` | 25 passed，无回归 |
| ascend_store helper 类 | 16 passed（含 transformer-layer bundle + 稀疏 indexer 变长布局）|
| `LayerBatchBuilder` 变长切片 | 端到端验证通过（indexer 层 GVA 覆盖 MLA+idx、非 indexer 层只覆盖 MLA、`page_size_bytes` 两层均匀）|
| ascend_store `Init`/`RegisterAndTransfer` 类 | 27 failed —— **pre-existing**（`use_layerwize` 旧 API 名漂移），非本次引入 |
| 上机（NPU）验证 | 待用户跑 `prefill_kv_offload_lxs.sh` |

**上机重点信号**：
1. 启动不再 `NotImplementedError: ...uniform number of cache legs...`。
2. diag 日志：`kv_caches` 条目数 > N，但 `group_num_layers[0] = N`，且**不再有** `KVPoolWorker: updated num_layers N -> 2N`。
3. step-end save drain 正常（无 `save drain not done` 超时）。
4. 相同 prompt 二次命中池 → 回灌 MLA+indexer，输出与无 offload baseline 逐 token 一致。
5. `layerwise_num_shared_buffers=2` 下不 OOM。

---

## 8. 待办

- **C8 对齐（`enable_sparse_c8=true`）**：用户要求「先适配 false」，C8 是下一步。新分支的 C8 开关会同时开 li C8 和 sfa C8，以新分支 C8 为主（旧分支 offload 的 C8 适配有问题）。当前变长布局与 leg 数无关（indexer 1 leg 或 2 leg 都行），C8 主要影响在 `page_bytes_per_token` / `compute_offload_sparse_c8_layout` 那条线，需单独适配 + 验证。
- **push 前跑 `bash format.sh ci`**：本环境无 pre-commit，未跑。HEAD 基线已有 10 个 pre-existing ruff 错误（均在 `model_runner_v1.py` 大文件内，非本次引入）；本次仅新增 1 个 `test_pool_worker.py` 的 import 排序 I001（test 文件，项目对此类 test import 本就容忍 `# noqa`）。
- **未跟踪文件** `tests/ut/kv_offload/a2/test_sfa_kv_offload_scheduler.py`：属 SFA **decode** offload 调度器（use_offload 路径），与本次 AscendStore **prefill** offload 不同子系统，未纳入本 commit，待单独处理。
